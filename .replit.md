modules = ["nodejs-24"]

run = "pnpm --filter @workspace/discord-bot run start"

[deployment]
router = "application"
deploymentTarget = "autoscale"
run = ["sh", "-c", "pnpm --filter @workspace/discord-bot run start"]

[deployment.build]
args = ["sh", "-c", "pnpm install && pnpm --filter @workspace/discord-bot run build"]

[deployment.postBuild]
args = ["pnpm", "store", "prune"]
env = { "CI" = "true" }

[workflows]

[[workflows.workflow]]
name = "Discord Bot"
author = "agent"
mode = "sequential"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "pnpm --filter @workspace/discord-bot run start"
cwd = "."
waitForPort = 8082

[workflows.workflow.tasks.env]
PORT = "8082"

[[ports]]
localPort = 8082
externalPort = 3000

[agent]
stack = "PNPM_WORKSPACE"
expertMode = true

[postMerge]
path = "scripts/post-merge.sh"
timeoutMs = 20000
